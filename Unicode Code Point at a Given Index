// Online C compiler to run C program online
#include <stdio.h>

int main() {
    char arr[]={"Geeks"};
    printf("string is %s\n",arr);
    
    for(int i=0;i<arr[i] !='\0';i++)
    {

    printf("The unique code at %d is %d\n",i,arr[i]);
    }
    return 0;
}
