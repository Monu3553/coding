// Online C compiler to run C program online
#include <stdio.h>
int sum(int a, int b)
{
    return a+b;
}
int main() 
{
    int a,b,c;
    a=2;
    b=3;
    
    c=sum(a,b);
    
    printf("sum is %d ",c);
    
    return 0;
}
