#include<stdio.h>
int main()
{
    int num;
    
    printf("Enter a number\n");
    scanf("%d", & num);
    
    if(num>=1)
    {
    printf("positive number");
    }
    
    else if(num<0)
    {
        printf("negative number");
    }
    else if(num==0)
    {
        printf("zero");
    }
}
