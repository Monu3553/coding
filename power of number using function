// Online C compiler to run C program online
#include <stdio.h>
int power(int x,int n)
{
 int pow=1;
    for(int i=0;i<n;i++)
    {
        pow=pow*x;
    }
    return pow;
}
int main() {
 int n=3,x=2,i,result;
    result=power(x,n);
    printf("%d",result);

    return 0;
}
